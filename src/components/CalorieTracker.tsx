import type { Activity } from "../types"


type CalorieTrackerProps = {
    state: Activity[]
}

export const CalorieTracker = ({state}: CalorieTrackerProps) => {
  return (
    <div>CalorieTracker</div>
  )
}
